name: android_contacts_plugin
description: A plugin for acces to android contacts
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.10
  json_serializable: ^6.1.6

flutter:
  plugin:
    platforms:
      android:
        package: com.brotherfolnciy.android_contacts_plugin
        pluginClass: AndroidContactsPlugin